/**
 * Construct a new EventStager instance. EventStager can be used to temporarily store events before saving them to the sink as a single commit.
 * @param {EventSink} The sink in which events will be saved as a single commit.
 * @param {String} streamID ID of the stream to which the events will belong.
 */
//TODO: find out if JSDoc 3 has an @implements tag.
//TODO: fix the type declarations to point to interfaces if possible, instead of floating names.
function EventStager(eventSink, streamID){
	
};